{
  "name": "ai-workflow-platform",
  "private": true,
  "type": "module",
  "workspaces": [
    "前端",
    "后端",
    "共享类型"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend \"npm run dev --workspace=@ai-workflow/backend\" \"npm run dev --workspace=@ai-workflow/frontend\"",
    "build": "npm run build --workspaces",
    "typecheck": "npm run build --workspace=@ai-workflow/shared-types && npm run typecheck --workspace=@ai-workflow/backend && npm run typecheck --workspace=@ai-workflow/frontend",
    "lint": "eslint 前端 后端 共享类型 --max-warnings=0",
    "test": "npm run build --workspace=@ai-workflow/shared-types && npm run test --workspace=@ai-workflow/backend && npm run test --workspace=@ai-workflow/frontend",
    "format:check": "prettier --check . --ignore-path .prettierignore"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "concurrently": "^9.1.2",
    "eslint": "^9.20.1",
    "eslint-plugin-vue": "^10.0.0",
    "globals": "^15.14.0",
    "prettier": "^3.5.3",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.24.0",
    "vite": "^6.1.0",
    "vitest": "^3.0.5",
    "vue": "^3.5.13",
    "vue-tsc": "^2.2.0"
  }
}
